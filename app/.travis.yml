language: android
android:
  components:
    - build-tools-29.0.2
    - android-29
env:
  global:
    - ADB_INSTALL_TIMEOUT = 8

script:
  - ./gradlew clean assembleDebug assembleRelease testDebug

branches:
  - develop-travis-test